{
  "name": "groundtruth-copilot",
  "version": "1.0.0",
  "description": "Safety-critical agricultural decision support system",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && esbuild index.tsx --bundle --outfile=dist/index.js --format=esm --jsx=automatic --external:react --external:react-dom --external:@google/genai && cp index.html dist/index.html",
    "start": "node server.js",
    "dev": "esbuild index.tsx --bundle --outfile=dist/index.js --format=esm --jsx=automatic --watch"
  },
  "dependencies": {
    "express": "^4.18.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@google/genai": "^1.34.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}